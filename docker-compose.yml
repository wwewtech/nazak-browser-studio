version: '3.8'

services:
  nazak-studio:
    build: .
    container_name: nazak-browser-studio
    restart: unless-stopped
    ports:
      - "8899:8899"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
